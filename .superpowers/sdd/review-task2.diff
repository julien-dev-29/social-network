# Review package: 89cd594..8da02e0

## Commits
8da02e0 feat: add SQLite connection and migration runner

## Files changed
 pkg/db/sqlite/sqlite.go | 48 ++++++++++++++++++++++++++++++++++++++++++++++++  1 file changed, 48 insertions(+)

## Diff
diff --git a/pkg/db/sqlite/sqlite.go b/pkg/db/sqlite/sqlite.go new file mode 100644 index 0000000..37ed53f --- /dev/null +++ b/pkg/db/sqlite/sqlite.go @@ -0,0 +1,48 @@ +package sqlite + +import ( +	"database/sql" +	"fmt" + +	_ "github.com/mattn/go-sqlite3" +	"github.com/golang-migrate/migrate/v4" +	migrateSqlite "github.com/golang-migrate/migrate/v4/database/sqlite" +	_ "github.com/golang-migrate/migrate/v4/source/file" +) + +func Connect(dbPath string) (*sql.DB, error) { +	db, err := sql.Open("sqlite3", dbPath) +	if err != nil { +		return nil, fmt.Errorf("failed to open database: %w", err) +	} + +	if err := db.Ping(); err != nil { +		return nil, fmt.Errorf("failed to ping database: %w", err) +	} + +	db.SetMaxOpenConns(1) + +	return db, nil +} + +func RunMigrations(db *sql.DB, migrationsPath string) error { +	driver, err := migrateSqlite.WithInstance(db, &migrateSqlite.Config{}) +	if err != nil { +		return fmt.Errorf("failed to create migration driver: %w", err) +	} + +	m, err := migrate.NewWithDatabaseInstance( +		migrationsPath, +		"sqlite", +		driver, +	) +	if err != nil { +		return fmt.Errorf("failed to create migrate instance: %w", err) +	} + +	if err := m.Up(); err != nil && err != migrate.ErrNoChange { +		return fmt.Errorf("failed to run migrations: %w", err) +	} + +	return nil +}
